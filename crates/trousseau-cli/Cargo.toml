[package]
name = "trousseau-cli"
version = "1.0.0-alpha.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false
description = "Command-line interface for the trousseau encrypted keyring."
keywords = ["secrets", "encryption", "cli", "keyring"]
categories = ["command-line-utilities"]

[[bin]]
name = "trousseau"
# The binary shares its name with the library crate; documenting both
# makes rustdoc write into the same target/doc/trousseau directory and
# race with itself. The library is the documented API.
doc = false
path = "src/main.rs"

[features]
default = ["clipboard"]
clipboard = ["dep:arboard"]

[lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"

[lints.clippy]
enum_glob_use = "deny"
pedantic = "deny"
nursery = "deny"
unwrap_used = "deny"
expect_used = "deny"
panic = "deny"
print_stdout = "warn"
print_stderr = "warn"

[dependencies]
trousseau = { path = "../trousseau" }
base64 = { workspace = true }
clap = { workspace = true }
clap_complete = { workspace = true }
clap_mangen = { workspace = true }
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
etcetera = { workspace = true }
rpassword = { workspace = true }
dialoguer = { workspace = true }
secrecy = { workspace = true }
age = { workspace = true }
human-panic = { workspace = true }
time = { workspace = true }
arboard = { workspace = true, optional = true }

[dev-dependencies]
assert_cmd = { workspace = true }
predicates = { workspace = true }
insta = { workspace = true }
tempfile = { workspace = true }
